services:
  - type: web
    name: smsb
    env: python
    plan: free
    pythonVersion: "3.13"          # Fuerza la versión de Python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py